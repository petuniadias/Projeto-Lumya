class Flights {
  destination = '';

  constructor(destination) {
    this.destination = destination;
  }
}

class Stays {
  title = '';
  rating = '';

  constructor(title, rating) {
    this.title = title;
    this.rating = rating;
  }
}

class Festivals {
  name = '';

  constructor(name) {
    this.name = name;
  }
}